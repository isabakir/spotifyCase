# Dependencies
   + Flask
   + requests(http request)
# Install
   + pip install flask
   + pip install requests
# Endpoint
   + /tracks/{genre}
   + GET request
   + json response
# Execute
  + flask run
  + Application run http://localhost:5000/

